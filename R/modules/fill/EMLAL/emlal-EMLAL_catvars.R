# EMLAL_templateDP.R

## 3. Create DP template
catvarsUI <- function(id, title, IM){
  ns <- NS(id)
  
  return(
    h1("Categorical Variables")
  )
}

catvars <- function(input, output, session, IM, savevar, globalRV){
  
}