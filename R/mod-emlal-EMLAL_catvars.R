# EMLAL_templateDP.R

## 3. Create DP template
catvarsUI <- function(id, title, dev = FALSE){
  ns <- NS(id)

  return(
    h1("Categorical Variables")
  )
}

catvars <- function(input, output, session, savevar, globals){

}
